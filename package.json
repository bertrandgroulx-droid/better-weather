{
  "name": "better-weather",
  "private": true,
  "version": "1.0.0",
  "description": "A phone-first weather app (forecast + animated radar map).",
  "scripts": {
    "test": "node tests/smoke.mjs"
  },
  "devDependencies": {
    "playwright": "^1.47.0"
  }
}
